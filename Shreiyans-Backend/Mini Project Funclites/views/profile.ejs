<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 text-white p-8">
        <a href="/logout" class="pb-10 inline-block text-red-400 hover:text-red-600">Logout</a>
    
        <h2 class="text-3xl mb-5">Profile Section</h2>
        <h2 class="text-xl font-semibold"><%= user.name %> 😊</h2>
    
        <p class="mt-5 text-lg">Create a Post</p>
        <form action="/post" method="POST" class="mt-3">
            <textarea class="text-white p-4 resize-none bg-transparent border-2 border-zinc-700 w-1/3" 
                placeholder="Write something..." 
                name="post_content" 
                id=""></textarea>
            <input type="submit" value="Create New Post" class="px-3 py-2 bg-blue-500 block mt-2 cursor-pointer rounded hover:bg-blue-600">
        </form>
    
        <h3 class="mt-8 text-2xl">Your Posts</h3>
        <div id="posts-container" class="mt-4 space-y-4">
            <% user.posts.forEach(post => { %>
                <div class="bg-zinc-700 p-4 rounded-md">
                    <p class="text-lg font-medium"><%= post.content %></p>
                    <span class="text-sm text-gray-400">Posted just now</span>
                    <div class="mt-2 flex gap-4">
                        <!-- Like button and displaying likes count -->
                        <a href="/like/<%= post._id %>" class="text-blue-400 hover:text-blue-600">Like</a>
                        <span class="text-sm text-gray-400"><%= post.likesCount %> Likes</span> <!-- Display likes count -->
                        <a href="/edit/<%= post._id %>" class="text-yellow-400 hover:text-yellow-600">Edit</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
